{% if resultObjects %} 
    <p>found {{ resultObjects|length }} object{{ resultObject|pluralize }}.</p>

    <ul>
        {% for curObject in resultObjects %}
        <li>{{ curObject.userPasswd }}</li>
        {% endfor %}
    </ul>
{% endif %}
